# DevOps home lab

Требования:

1. sudo должен быть без пароля ALL=(ALL) NOPASSWD: ALL
2. установленный virt-manager: sudo apt install virt-manager
3. установленные kubectl, helm, talosctl, systemd-timesyncd
